<%= form_for(@joystick) do |f| %>
  <% if @joystick.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@joystick.errors.count, "error") %> prohibited this joystick from being saved:</h2>

      <ul>
      <% @joystick.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :x %><br>
    <%= f.text_field :x %>
  </div>
  <div class="field">
    <%= f.label :y %><br>
    <%= f.text_field :y %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
